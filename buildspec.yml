version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mvn compile
      - cp target/Hello.war Hello.war
  post_build:
    commands:
      - mvn package
      - mkdir artifact  
      - unzip target/my.war -d artifact/    
artifacts:
  files:
    - artifact/**/*
    name: artifact
cache:
    paths:
        - '/root/.m2/**/*'

  
        
